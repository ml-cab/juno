A. This requires high importance. Think hard of it as an ML engineer and Java software architect.

B. Up-to speed yourself with the @README.md, @CHANGELOG.md and all the files in @docs folder

C. Read the `.sh` and `.bat` files from @scripts folder recursivly.

D. follow this implementation rools:

    0 - if you are uncertain - you are welcome to ask instead of going onto wrong direction. User will reply questions.
    1 - unit tests first, only for valuable business logic
    2 - implementation details are designed with performance in mind
    3 - implementation follows KISS
    4 - it's better to add new java classes than extend existing once
    5 - (if applicable) update @docs/agent-arch.txt, @docs/howto.md, @README.md
    6 - Do not try to run it yourself. User will do it for you.
    7 - Code-review yourself then present me files for test run
    8 - no emojis, be strict and precise
    9 - no need to execute tests yourself. just code review and present files. User will be testing.
    10 - No em-dashes. It is not allowed to use in any textual documentation you are writing. In paragraph use `.` instead; in lists use `:`; in sentence use `because` or `due to` or eth. based on context.

E. output - just list all the files for preview, never zip me files back!
